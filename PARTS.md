# Parts library index

Generated by `scripts/reindex.py` — do not edit by hand. Import path and definition location are in `parts.json`.

**30 components** in 4 categories. Versions are per-component semver.

## patterns

### `patterns.din_rail_ts35_profile` v1.0.0

TS35 top-hat DIN rail cross-section (35 mm wide) as a sketch in XY, for subtracting a rail pocket from a clip.

- **import:** `from lib.patterns.din_rail import din_rail_ts35_profile`  
- **returns:** `Sketch`  
- **tags:** clip, din rail, electrical, en 60715, enclosure, profile, ts35  
- **validated in:** UNVALIDATED  
- **orientation:** clip printed with rail axis vertical (Z) so the lips are solid perimeters  
- **notes:** UNVALIDATED: geometry from standard, no test print yet  

| param | type | units | default | description |
|---|---|---|---|---|
| `depth` | float | mm | `7.5` | rail depth: 7.5 (standard) or 15 |
| `clearance` | float | mm | `0.3` | grown outward so the clip slides on |
| `sheet_t` | float | mm | `1.0` | rail sheet-metal thickness |

```python
clip = clip - extrude(din_rail_ts35_profile(clearance=0.3), amount=clip_len)
```

### `patterns.esp32_footprint` v1.0.0

ESP32 dev-board PCB outline (no mount holes exist on these boards) centred on the origin, USB end at -X.

- **import:** `from lib.patterns.esp32 import esp32_footprint`  
- **returns:** `Sketch`  
- **tags:** devkitc, doit, esp32, esp32-c3, esp32-s3, footprint, microcontroller, outline, pcb, supermini  
- **validated in:** PLA  
- **orientation:** any (2D pattern)  
- **notes:** Boards have no holes: hold by edge slots (pcb_slot_cradle) or header sockets.  

| param | type | units | default | description |
|---|---|---|---|---|
| `board` | str | enum | `"devkitc_v4"` | devkitc_v4, s3_devkitc_1, nodemcu_32s, doit_v1_30, c3_supermini |
| `clearance` | float | mm | `0.0` | grown outward on all sides |

```python
pocket = extrude(esp32_footprint("devkitc_v4", clearance=0.5), amount=pcb_t)
```

### `patterns.esp32_header_rows` v1.0.0

Two rectangular strips where the ESP32 dev-board header rows sit (for pin-clearance pockets or socket recesses).

- **import:** `from lib.patterns.esp32 import esp32_header_rows`  
- **returns:** `Sketch`  
- **tags:** 2.54, esp32, header, keep-out, pins, pocket  
- **validated in:** PLA  
- **orientation:** any (2D pattern)  

| param | type | units | default | description |
|---|---|---|---|---|
| `board` | str | enum | `"devkitc_v4"` | devkitc_v4, s3_devkitc_1, nodemcu_32s, doit_v1_30, c3_supermini |
| `strip_width` | float | mm | `3.0` | width of each strip across the pins |
| `extra_length` | float | mm | `1.0` | added to the strip length beyond the pin run |

```python
floor = floor - extrude(esp32_header_rows(), amount=pin_len)
```

### `patterns.fan_mount` v1.0.0

Axial fan four-hole pattern plus optional circular airflow cutout, centred on the origin.

- **import:** `from lib.patterns.fan import fan_mount`  
- **returns:** `Sketch`  
- **tags:** 120mm, 30mm, 40mm, 80mm, cooling, cutout, fan, holes, vent  
- **validated in:** PLA, PETG  
- **orientation:** any (2D pattern)  

| param | type | units | default | description |
|---|---|---|---|---|
| `size` | int | mm | `30` | fan frame size: 25, 30, 40, 50, 60, 70, 80, 92, 120, 140 |
| `hole_d` | float | None | mm | `auto` | screw hole diameter; default per fan size (3.2 for 30 mm M3, 4.3-4.5 for 60 mm+) |
| `cutout` | bool | bool | `True` | include the circular airflow opening |
| `cutout_d` | float | None | mm | `auto` | airflow opening diameter; default = size - 2 |
| `print_oversize` | float | mm | `0.2` | FDM oversize added to the screw holes |

```python
side = Plane.XZ * (Rectangle(w, h) - fan_mount(30))
```

### `patterns.pi4_mount` v1.0.0

Raspberry Pi 4 B (and 3 B/B+) four-hole mount pattern (58 x 49 mm, M2.5).

- **import:** `from lib.patterns.pi import pi4_mount`  
- **returns:** `Sketch`  
- **tags:** M2.5, holes, mount, pi 4, pi3, pi4, raspberry pi, sbc  
- **validated in:** PLA, PETG  
- **orientation:** any (2D pattern)  

| param | type | units | default | description |
|---|---|---|---|---|
| `hole_d` | float | None | mm | `auto` | hole diameter before print compensation; default = M2.5 clearance (2.7) |
| `print_oversize` | float | mm | `0.2` | added to diameter because FDM holes print small |

```python
floor = Rectangle(95, 66) - pi4_mount()
```

### `patterns.pi5_mount` v1.0.0

Raspberry Pi 5 four-hole mount pattern (58 x 49 mm, M2.5) centred on the origin.

- **import:** `from lib.patterns.pi import pi5_mount`  
- **returns:** `Sketch`  
- **tags:** M2.5, active cooler, holes, mount, pi 5, pi5, raspberry pi, sbc  
- **validated in:** PLA, PETG  
- **orientation:** any (2D pattern)  

| param | type | units | default | description |
|---|---|---|---|---|
| `hole_d` | float | None | mm | `auto` | hole diameter before print compensation; default = M2.5 clearance (2.7) |
| `print_oversize` | float | mm | `0.2` | added to diameter because FDM holes print small |
| `active_cooler_holes` | bool | bool | `False` | also cut the two 3 mm Active Cooler push-pin holes |

```python
floor = Rectangle(95, 66) - pi5_mount()
bosses = [loc * heat_set_boss("M2.5") for loc in locations_of(pi5_mount())]
```

### `patterns.pi_board_outline` v1.0.0

Raspberry Pi PCB outline (rounded rectangle) positioned to line up with the matching mount pattern.

- **import:** `from lib.patterns.pi import pi_board_outline`  
- **returns:** `Sketch`  
- **tags:** footprint, keep-out, outline, pcb, pi zero, pi4, pi5, raspberry pi  
- **validated in:** PLA, PETG  
- **orientation:** any (2D pattern)  

| param | type | units | default | description |
|---|---|---|---|---|
| `board` | str | enum | `"pi5"` | pi5, pi4, pi3 or pi_zero |
| `clearance` | float | mm | `0.0` | grown outward on every side (use for pocket/keep-out) |

```python
pocket = extrude(pi_board_outline("pi5", clearance=1.0), amount=pcb_t)
```

### `patterns.pi_zero_mount` v1.0.0

Raspberry Pi Zero / Zero 2 W four-hole mount pattern (58 x 23 mm, M2.5).

- **import:** `from lib.patterns.pi import pi_zero_mount`  
- **returns:** `Sketch`  
- **tags:** M2.5, holes, mount, pi zero, raspberry pi, sbc, zero 2 w  
- **validated in:** PLA, PETG  
- **orientation:** any (2D pattern)  

| param | type | units | default | description |
|---|---|---|---|---|
| `hole_d` | float | None | mm | `auto` | hole diameter before print compensation; default = M2.5 clearance (2.7) |
| `print_oversize` | float | mm | `0.2` | added to diameter because FDM holes print small |

```python
floor = Rectangle(75, 40) - pi_zero_mount()
```

### `patterns.vesa_mount` v1.0.0

VESA MIS-D/E four-hole pattern (75, 100, 200x100, 200) centred on the origin.

- **import:** `from lib.patterns.vesa import vesa_mount`  
- **returns:** `Sketch`  
- **tags:** M4, M6, holes, monitor, mount, tv, vesa  
- **validated in:** PETG  
- **orientation:** any (2D pattern); print the bracket so screw load is in-plane  

| param | type | units | default | description |
|---|---|---|---|---|
| `pattern` | str | enum | `"100"` | 75, 100, 200x100 or 200 |
| `hole_d` | float | None | mm | `auto` | hole diameter; default = M4 medium clearance (4.5) or M6 (6.6) |
| `print_oversize` | float | mm | `0.2` | FDM oversize |

```python
plate = RectangleRounded(120, 120, 5) - vesa_mount("100")
```

## mechanisms

### `mechanisms.cantilever_latch` v1.0.0

Cantilever snap-fit hook: flexing beam along +Z from z=0 with a ramped hook on the +X face at the tip.

- **import:** `from lib.mechanisms.cantilever_latch import cantilever_latch`  
- **returns:** `Part`  
- **tags:** cantilever, clip, compliant, hook, latch, lid, snap, snap-fit  
- **validated in:** PETG  
- **orientation:** beam lying flat in the bed plane (beam length along X or Y, thickness along Z). Never print the beam standing up: bending then peels layers apart and the root cracks.  
- **notes:** PETG and ABS/ASA flex reliably; PLA snaps below ~1.2 mm thickness or after a few dozen cycles. Strain at the root ~ 1.5*t*hook_depth/length^2 — keep under ~2% for PETG. Defaults give ~1.0%.  

| param | type | units | default | description |
|---|---|---|---|---|
| `length` | float | mm | `12.0` | beam length from root (z=0) to the underside of the hook |
| `width` | float | mm | `6.0` | beam width (Y) |
| `thickness` | float | mm | `1.6` | beam thickness (X) — the flexing dimension |
| `hook_depth` | float | mm | `1.2` | how far the hook protrudes in +X beyond the beam face = deflection required to release |
| `hook_height` | float | mm | `2.5` | height of the hook block (Z) |
| `entry_angle` | float | deg | `30.0` | ramp angle on the top of the hook (lower = easier to push closed) |
| `retention_angle` | float | deg | `90.0` | angle of the underside of the hook (90 = permanent, 45 = easy release) |
| `root_fillet` | float | mm | `0.5` | fillet at the beam root to spread stress |

```python
latch = cantilever_latch(length=10, thickness=1.6)
lid = lid + Pos(x, y, lid_t) * Rot(0, 0, 180) * latch     # hook facing inward
body = body - Pos(x, y, z) * latch_window(width=6, hook_depth=1.2)
```

### `mechanisms.latch_window` v1.0.0

Negative (subtract me) rectangular window through a wall for a cantilever_latch hook to snap into.

- **import:** `from lib.mechanisms.cantilever_latch import latch_window`  
- **returns:** `Part`  
- **tags:** catch, latch, lid, negative, snap, window  
- **validated in:** PETG  
- **orientation:** beam lying flat in the bed plane (beam length along X or Y, thickness along Z). Never print the beam standing up: bending then peels layers apart and the root cracks.  
- **notes:** PETG and ABS/ASA flex reliably; PLA snaps below ~1.2 mm thickness or after a few dozen cycles. Strain at the root ~ 1.5*t*hook_depth/length^2 — keep under ~2% for PETG. Defaults give ~1.0%.  

| param | type | units | default | description |
|---|---|---|---|---|
| `width` | float | mm | `6.0` | latch beam width |
| `hook_depth` | float | mm | `1.2` | latch hook_depth |
| `hook_height` | float | mm | `2.5` | latch hook_height |
| `clearance` | float | mm | `0.3` | added around the hook (0.3 slip fit) |
| `wall_t` | float | mm | `1.6` | wall thickness to punch through (window depth in X, both ways) |

```python
body = body - Pos(wall_x, y, z_hook) * latch_window(6, 1.2, 2.5)
```

### `mechanisms.living_hinge_lattice` v1.0.0

Lattice (kerf-style) living hinge: staggered slots cut through a panel so it can bend about the Y axis; returns the slot pattern to subtract.

- **import:** `from lib.mechanisms.living_hinge import living_hinge_lattice`  
- **returns:** `Sketch`  
- **tags:** bend, compliant, flexible, hinge, kerf, lattice, living hinge  
- **validated in:** PLA, PETG  
- **orientation:** flat on the bed; slot walls vertical. Works even in PLA because links twist rather than bend across layers.  
- **notes:** Bend radius ~ length / total bend angle; more columns = gentler bend.  

| param | type | units | default | description |
|---|---|---|---|---|
| `length` | float | mm | `20.0` | bend region length along X (direction of bending) |
| `width` | float | mm | `40.0` | panel width along the hinge axis (Y) |
| `slot_w` | float | mm | `1.0` | slot width (X) |
| `slot_gap` | float | mm | `3.0` | gap between consecutive slots along Y in the same column (the torsion links) |
| `column_pitch` | float | mm | `3.0` | X pitch between slot columns |
| `link_w` | float | mm | `1.6` | solid material between slots in Y = torsion link width |

```python
plate = plate - extrude(living_hinge_lattice(20, 40), amount=plate_t)
```

### `mechanisms.living_hinge_web` v1.0.0

Thin flexible web joining two thicker panels (single-strip living hinge), hinge axis along Y, printed flat.

- **import:** `from lib.mechanisms.living_hinge import living_hinge_web`  
- **returns:** `Part`  
- **tags:** compliant, flexible, fold, hinge, lid, living hinge, web  
- **validated in:** PETG, TPU  
- **orientation:** flat on the bed, hinge axis in the bed plane; the web is then 1-2 continuous layers with extrusion lines along the axis  
- **notes:** PLA fatigue-cracks within tens of cycles; PP and TPU are best. Fold immediately after printing while warm for PETG.  

| param | type | units | default | description |
|---|---|---|---|---|
| `width` | float | mm | `30.0` | hinge width along the axis (Y) |
| `web_thickness` | float | mm | `0.5` | flexing web thickness (1-2 layers: 0.4-0.6) |
| `web_length` | float | mm | `2.0` | web span between the panels (X) |
| `panel_t` | float | mm | `1.6` | thickness of the joined panels |
| `panel_len` | float | mm | `5.0` | length of each stub panel included (X) — union your real panels onto these |
| `fillet_r` | float | mm | `0.3` | fillet where the web meets the panels (spreads strain) |

```python
hinge = living_hinge_web(width=40, web_thickness=0.5)
part = left_panel + hinge + right_panel
```

### `mechanisms.parallel_flexure` v1.0.0

Parallel-beam flexure stage: fixed block and moving block joined by N thin beams; moving block translates in Y.

- **import:** `from lib.mechanisms.flexure import parallel_flexure`  
- **returns:** `Part`  
- **tags:** button, compliant, flexure, linear guide, parallel, spring, stage  
- **validated in:** PETG  
- **orientation:** flat on the bed with beams in the XY plane so bending stress runs along the extrusion lines, not across layers  
- **notes:** Stiffness ~ N*E*depth*t^3/L^3. Halve thickness for 8x softer. PLA works for tiny strokes only.  

| param | type | units | default | description |
|---|---|---|---|---|
| `beam_length` | float | mm | `20.0` | beam span along X between the blocks |
| `beam_thickness` | float | mm | `1.0` | beam thickness (Y) — flexing dimension, 0.8-1.2 |
| `beam_count` | int | count | `2` | number of parallel beams (2 = classic parallelogram) |
| `beam_spacing` | float | mm | `8.0` | Y pitch between beams |
| `depth` | float | mm | `6.0` | part depth (Z), i.e. beam height |
| `block_w` | float | mm | `6.0` | X width of the two end blocks |

```python
stage = parallel_flexure(beam_length=25, beam_thickness=0.8)
button = stage + Pos(x_moving_block, 0, 0) * button_cap
```

### `mechanisms.snap_groove` v1.0.0

Negative (subtract me) half-round groove matching snap_ridge, cut into a wall face at y=0 going into -Y.

- **import:** `from lib.mechanisms.snap_fit import snap_groove`  
- **returns:** `Part`  
- **tags:** compliant, detent, groove, lid, negative, snap  
- **validated in:** PETG, PLA  
- **orientation:** ridge running horizontally along a vertical wall prints fine (the half-round is a shallow overhang)  
- **notes:** Interference 0.4-0.6 mm total for PETG walls 1.2-1.6 thick; PLA prefers <=0.4.  

| param | type | units | default | description |
|---|---|---|---|---|
| `length` | float | mm | `20.0` | groove length along X |
| `r` | float | mm | `0.6` | matching ridge radius |
| `clearance` | float | mm | `0.1` | added to radius so the bead seats without binding |

```python
body = body - Pos(0, inner_wall_y, z) * snap_groove(length=lip_len, r=0.6)
```

### `mechanisms.snap_ridge` v1.0.0

Half-round snap ridge (bead) running along X, protruding in +Y from a wall face at y=0; sits on a lid lip.

- **import:** `from lib.mechanisms.snap_fit import snap_ridge`  
- **returns:** `Part`  
- **tags:** bead, compliant, detent, friction, lid, ridge, snap  
- **validated in:** PETG, PLA  
- **orientation:** ridge running horizontally along a vertical wall prints fine (the half-round is a shallow overhang)  
- **notes:** Interference 0.4-0.6 mm total for PETG walls 1.2-1.6 thick; PLA prefers <=0.4.  

| param | type | units | default | description |
|---|---|---|---|---|
| `length` | float | mm | `20.0` | ridge length along X |
| `r` | float | mm | `0.6` | bead radius (protrusion) |
| `end_taper` | float | mm | `0.0` | reserved (0) |

```python
lip = lip + Pos(0, lip_face_y, z) * snap_ridge(length=lip_len - 4, r=0.6)
```

## fasteners

### `fasteners.captive_nut_slot` v1.0.0

Negative (subtract me) side-entry slot for a captive hex nut plus the screw clearance hole.

- **import:** `from lib.fasteners.captive_nut import captive_nut_slot`  
- **returns:** `Part`  
- **tags:** M3, M4, captive, hex, negative, nut, slot, trap  
- **validated in:** PETG, PLA  
- **orientation:** nut axis vertical; if the slot roof is a horizontal bridge keep slot_length short or add a sacrificial layer  

| param | type | units | default | description |
|---|---|---|---|---|
| `size` | str | enum | `"M3"` | nut size: M2, M2.5, M3, M4, M5 |
| `slot_length` | float | mm | `10.0` | how far the entry slot runs from the nut centre in +X (reach the part's edge) |
| `nut_clearance` | float | mm | `0.3` | added across flats so the nut slides in |
| `thickness_clearance` | float | mm | `0.3` | added to nut thickness |
| `screw_depth_above` | float | mm | `10.0` | screw clearance hole length in +Z above the nut |
| `screw_depth_below` | float | mm | `10.0` | screw clearance hole length in -Z below the nut |
| `screw_oversize` | float | mm | `0.3` | FDM oversize on the screw hole |

```python
body = body - Pos(x, y, z_nut) * captive_nut_slot("M3", slot_length=wall_to_edge)
```

### `fasteners.clearance_hole` v1.0.0

Negative (subtract me) clearance hole for a metric screw, with print oversize and optional counterbore or countersink.

- **import:** `from lib.fasteners.clearance_hole import clearance_hole`  
- **returns:** `Part`  
- **tags:** M2, M2.5, M3, M4, clearance, counterbore, countersink, hole, negative, screw  
- **validated in:** PLA, PETG  
- **orientation:** any; for horizontal holes use primitives.teardrop instead  
- **used by:** models/esp32_devkit_case/model.py  

| param | type | units | default | description |
|---|---|---|---|---|
| `size` | str | enum | `"M3"` | screw size: M2, M2.5, M3, M4, M5 |
| `depth` | float | mm | `20.0` | hole length along -Z from the surface at z=0 (make it through the whole part) |
| `fit` | str | enum | `"medium"` | ISO 273 series: fine, medium, coarse |
| `print_oversize` | float | mm | `0.2` | added to diameter because FDM holes print small (0.2-0.4 typical) |
| `head` | str | enum | `"none"` | none, counterbore (socket head) or countersink (90 deg) |
| `head_clearance` | float | mm | `0.4` | added to head diameter for counterbore/countersink |
| `head_depth` | float | None | mm | `auto` | counterbore depth; default = head height |

```python
plate = plate - [Pos(x, y, plate_t) * clearance_hole("M3", depth=plate_t) for x, y in pts]
```

### `fasteners.heat_set_boss` v1.0.0

Cylindrical boss with a correctly sized pocket for a brass heat-set insert.

- **import:** `from lib.fasteners.heat_set_boss import heat_set_boss`  
- **returns:** `Part`  
- **tags:** M2, M2.5, M3, M4, boss, heat-set, insert, screw, standoff  
- **validated in:** PETG, PLA  
- **orientation:** boss axis vertical (insert pressed in along Z); pocket walls are then continuous perimeters  
- **notes:** Insert pocket is sized per the insert spec, never per the screw. In PLA keep iron at ~200 C.  
- **used by:** models/esp32_devkit_case/model.py  

| param | type | units | default | description |
|---|---|---|---|---|
| `size` | str | enum | `"M3"` | insert size: M2, M2.5, M3, M4, M5 |
| `height` | float | mm | `8.0` | total boss height from its base (z=0) to the insert face |
| `wall` | float | None | mm | `auto` | plastic wall around the insert hole; defaults to the insert's minimum |
| `pocket_depth` | float | None | mm | `auto` | insert pocket depth; default = insert length + 1 mm for melt relief |
| `top_chamfer` | float | mm | `0.4` | lead-in chamfer at the mouth of the pocket (helps the insert seat square) |
| `base_fillet` | float | mm | `0.0` | fillet where the boss meets its parent surface (0 = none) |
| `through` | bool | bool | `False` | make the pocket a through hole (for bosses on thin floors) |

```python
from lib.component import locations_of
from lib.patterns.pi import pi5_mount
bosses = Part() + [loc * heat_set_boss("M2.5", height=6) for loc in locations_of(pi5_mount())]
floor = floor + bosses
```

### `fasteners.heat_set_pocket` v1.0.0

Negative (subtract me) pocket for a heat-set insert in a solid wall or floor.

- **import:** `from lib.fasteners.heat_set_boss import heat_set_pocket`  
- **returns:** `Part`  
- **tags:** heat-set, hole, insert, negative, pocket  
- **validated in:** PETG, PLA  
- **orientation:** pocket axis vertical preferred  

| param | type | units | default | description |
|---|---|---|---|---|
| `size` | str | enum | `"M3"` | insert size: M2, M2.5, M3, M4, M5 |
| `depth` | float | None | mm | `auto` | pocket depth; default = insert length + 1 mm |
| `extra` | float | mm | `1.0` | extra height above z=0 so the cutter clears the surface cleanly |

```python
lid = lid - [loc * heat_set_pocket("M3") for loc in locations_of(holes)]
```

### `fasteners.screw_boss` v1.0.0

Boss with a pilot hole for a machine screw to self-thread into printed plastic.

- **import:** `from lib.fasteners.screw_boss import screw_boss`  
- **returns:** `Part`  
- **tags:** M2, M2.5, M3, M4, boss, pilot, screw, self-tapping, thread-forming  
- **validated in:** PETG  
- **orientation:** boss axis vertical so the screw threads cut across layers, not between them  
- **notes:** Good for ~5 insert/remove cycles. Use heat_set_boss for anything serviced often. PLA cracks under over-torque.  

| param | type | units | default | description |
|---|---|---|---|---|
| `size` | str | enum | `"M3"` | screw size: M2, M2.5, M3, M4, M5 |
| `height` | float | mm | `8.0` | boss height from z=0 |
| `wall` | float | mm | `1.6` | wall around the pilot (>= 2 perimeters, 1.6 mm recommended for M3) |
| `pilot_d` | float | None | mm | `auto` | pilot hole diameter; default from hardware table (thread-forming size) |
| `pilot_depth` | float | None | mm | `auto` | pilot depth; default = full height minus one wall thickness of floor |
| `top_chamfer` | float | mm | `0.3` | entry chamfer |
| `base_fillet` | float | mm | `0.0` | fillet at the base (0 = none) |

```python
boss = screw_boss("M3", height=10)
body = body + Pos(20, 10, floor_t) * boss
```

## primitives

### `primitives.box_lid` v1.0.0

Flat lid for rounded_box with an inner lip that drops inside the walls; printed upside down (top face on the bed).

- **import:** `from lib.primitives.rounded_box import box_lid`  
- **returns:** `Part`  
- **tags:** box, cover, enclosure, lid, lip  
- **validated in:** PLA, PETG  
- **orientation:** top face down on the bed, lip pointing up  
- **used by:** models/esp32_devkit_case/model.py  

| param | type | units | default | description |
|---|---|---|---|---|
| `length` | float | mm | `100.0` | outer X of the box it fits |
| `width` | float | mm | `70.0` | outer Y of the box it fits |
| `wall` | float | mm | `1.6` | box wall thickness |
| `corner_r` | float | mm | `3.0` | box outer corner radius |
| `thickness` | float | mm | `1.6` | lid plate thickness |
| `lip_height` | float | mm | `3.0` | how far the lip drops into the box |
| `lip_thickness` | float | mm | `1.2` | lip wall thickness |
| `clearance` | float | mm | `0.25` | gap per side between lip and box inner wall (slip fit 0.2-0.3) |
| `top_chamfer` | float | mm | `0.4` | chamfer on the outer top edge (bed-contact edge when printed inverted) |

```python
lid = box_lid(110, 80, wall=2.0)
```

### `primitives.cable_grommet` v1.0.0

Cable pass-through cutout: teardrop hole for the cable plus a keyhole slit to the panel edge so a pre-terminated cable can be slipped in.

- **import:** `from lib.primitives.cable_grommet import cable_grommet`  
- **returns:** `Sketch`  
- **tags:** cable, grommet, keyhole, pass-through, slot, strain relief  
- **validated in:** PETG  
- **orientation:** hole axis horizontal, slit pointing up  
- **used by:** models/esp32_devkit_case/model.py  

| param | type | units | default | description |
|---|---|---|---|---|
| `cable_d` | float | mm | `5.0` | cable outer diameter |
| `clearance` | float | mm | `0.6` | added to diameter |
| `slit_w` | float | mm | `0.0` | slit width (0 = no slit, plain teardrop) |
| `slit_len` | float | mm | `10.0` | slit length from hole centre in +Y (reach the panel edge) |

```python
back = back - extrude(Plane.XZ * Pos(0, 10) * cable_grommet(4.0, slit_w=2.0, slit_len=12), amount=wall, both=True)
```

### `primitives.foot` v1.0.0

Cylindrical foot / bumper standing on z=0 with a chamfered bed edge.

- **import:** `from lib.primitives.feet import foot`  
- **returns:** `Part`  
- **tags:** base, bumper, feet, foot, standoff  
- **validated in:** PLA, PETG, TPU  
- **orientation:** printed as part of the body, bed side down  

| param | type | units | default | description |
|---|---|---|---|---|
| `d` | float | mm | `8.0` | foot diameter |
| `height` | float | mm | `2.0` | foot height |
| `bed_chamfer` | float | mm | `0.4` | chamfer on the bed-contact edge |

```python
body = body + [loc * foot() for loc in GridLocations(80, 50, 2, 2)]
```

### `primitives.l_bracket` v1.0.0

90-degree L (angle) bracket with a row of screw clearance holes along each arm, standing on its side on z=0 (strongest print orientation).

- **import:** `from lib.primitives.l_bracket import l_bracket`  
- **returns:** `Part`  
- **tags:** L, M3, M4, angle, bracket, corner, holes, mount, structural  
- **validated in:** UNVALIDATED  
- **orientation:** on its side: L profile in the bed plane, width along Z, so the corner is loaded along the layers; holes are horizontal (teardrop point up)  
- **notes:** UNVALIDATED: no test print yet. Printing one arm flat on the bed puts the corner's bending load across layers.  
- **used by:** models/l_bracket_40/model.py  

| param | type | units | default | description |
|---|---|---|---|---|
| `arm_a` | float | mm | `40.0` | arm along +X, measured from the outer corner to the free end |
| `arm_b` | float | mm | `40.0` | arm along +Y, measured from the outer corner to the free end |
| `thickness` | float | mm | `4.0` | arm thickness (multiple of 0.4; >= 3 for M3 hardware) |
| `width` | float | mm | `20.0` | bracket width = print height along Z |
| `size` | str | enum | `"M3"` | screw size for the holes: M2, M2.5, M3, M4, M5 |
| `holes_per_arm` | int | count | `3` | holes in the single row on each arm (0 = plain bracket) |
| `hole_pitch` | float | None | mm | `auto` | centre-to-centre along the arm; default spreads the holes evenly over the arm span beyond the other arm |
| `hole_style` | str | enum | `"teardrop"` | teardrop (horizontal hole bridges without support) or round |
| `fit` | str | enum | `"medium"` | ISO 273 clearance series: fine, medium, coarse |
| `print_oversize` | float | mm | `0.2` | added to hole diameter because FDM holes print small |
| `inner_r` | float | mm | `1.0` | fillet radius on the inside corner (vertical edge; stress relief, 0 = sharp) |
| `outer_r` | float | mm | `0.0` | fillet radius on the outside corner (0 = sharp) |
| `bottom_chamfer` | float | mm | `0.4` | chamfer on the bed-contact edge loop (elephant foot, 0 = none) |

```python
bracket = l_bracket(40, 40, thickness=4, width=20, size="M3", holes_per_arm=3)
plate = plate + Pos(x, y, 0) * l_bracket(30, 20, holes_per_arm=2)
```

### `primitives.pcb_slot_cradle` v1.0.0

Pair of rails with slots that grip a PCB by its two long edges (for boards with no mount holes), centred on the origin.

- **import:** `from lib.primitives.pcb_cradle import pcb_slot_cradle`  
- **returns:** `Part`  
- **tags:** card guide, cradle, esp32, holder, pcb, rails, slot  
- **validated in:** PLA  
- **orientation:** rails vertical; slot faces are then vertical walls, no bridging  
- **notes:** Board slides in from the +X end; leave that end open in the enclosure or make one rail cantilevered.  
- **used by:** models/esp32_devkit_case/model.py  

| param | type | units | default | description |
|---|---|---|---|---|
| `board_l` | float | mm | `54.4` | PCB length along the rails (X) |
| `board_w` | float | mm | `27.9` | PCB width across the rails (Y) |
| `pcb_t` | float | mm | `1.6` | PCB thickness |
| `rail_h` | float | mm | `6.0` | total rail height |
| `rail_t` | float | mm | `1.6` | rail wall thickness outside the slot |
| `slot_depth` | float | mm | `1.5` | how far the slot bites in over each edge |
| `clearance` | float | mm | `0.3` | added to pcb_t (slot height) and to board_w (slot span) |
| `slot_z` | float | mm | `3.0` | height of the slot floor above z=0 |

```python
cradle = pcb_slot_cradle(*esp32_dims)
body = body + Pos(0, 0, floor_t) * cradle
```

### `primitives.rounded_box` v1.0.0

Open-top rounded rectangular shell (enclosure body) with floor, standing on z=0; outer dimensions given.

- **import:** `from lib.primitives.rounded_box import rounded_box`  
- **returns:** `Part`  
- **tags:** body, box, case, enclosure, rounded, shell  
- **validated in:** PLA, PETG  
- **orientation:** floor on the bed, open top up  
- **used by:** models/esp32_devkit_case/model.py  

| param | type | units | default | description |
|---|---|---|---|---|
| `length` | float | mm | `100.0` | outer X |
| `width` | float | mm | `70.0` | outer Y |
| `height` | float | mm | `30.0` | outer Z including floor |
| `wall` | float | mm | `1.6` | side wall thickness (multiple of 0.4) |
| `floor_t` | float | None | mm | `auto` | floor thickness; default = wall |
| `corner_r` | float | mm | `3.0` | outer vertical corner radius |
| `bottom_chamfer` | float | mm | `0.4` | chamfer on the bed-contact edge (elephant foot) |
| `inner_fillet` | float | mm | `0.0` | fillet where floor meets the inner walls (0 = none) |

```python
body = rounded_box(110, 80, 35, wall=2.0)
body = body + Pos(0, 0, floor_t) * bosses
```

### `primitives.rubber_foot_recess` v1.0.0

Negative (subtract me) shallow recess to locate a stick-on rubber foot; top face at z=0, extends down.

- **import:** `from lib.primitives.feet import rubber_foot_recess`  
- **returns:** `Part`  
- **tags:** foot, negative, pocket, recess, rubber  
- **validated in:** PLA  
- **orientation:** any  

| param | type | units | default | description |
|---|---|---|---|---|
| `d` | float | mm | `10.0` | rubber foot diameter |
| `depth` | float | mm | `0.8` | recess depth |
| `clearance` | float | mm | `0.4` | added to diameter |

```python
body = body - [loc * rubber_foot_recess(10) for loc in GridLocations(80, 50, 2, 2)]
```

### `primitives.teardrop` v1.0.0

Teardrop hole profile (circle with a pointed top) so a horizontal-axis hole bridges without support; subtract from a vertical wall.

- **import:** `from lib.primitives.cable_grommet import teardrop`  
- **returns:** `Sketch`  
- **tags:** bridge, cable, hole, horizontal, overhang, teardrop  
- **validated in:** PLA, PETG  
- **orientation:** hole axis horizontal, point up (+Y in the sketch)  

| param | type | units | default | description |
|---|---|---|---|---|
| `d` | float | mm | `6.0` | nominal hole diameter |
| `angle` | float | deg | `45.0` | half-angle of the point (45 = printable without support) |
| `print_oversize` | float | mm | `0.3` | FDM oversize |

```python
wall = wall - extrude(Plane.XZ * Pos(x, z) * teardrop(cable_d), amount=wall_t, both=True)
```

### `primitives.vent_slots` v1.0.0

Grid of rounded ventilation slots filling a rectangular area, centred on the origin (subtract from a wall).

- **import:** `from lib.primitives.vented_panel import vent_slots`  
- **returns:** `Sketch`  
- **tags:** airflow, cooling, grille, panel, slots, vent  
- **validated in:** PLA, PETG  
- **orientation:** on a vertical wall run slots vertically so they need no bridging  
- **used by:** models/esp32_devkit_case/model.py  

| param | type | units | default | description |
|---|---|---|---|---|
| `area_l` | float | mm | `40.0` | length of the vented area along the slots |
| `area_w` | float | mm | `20.0` | width across the slots |
| `slot_w` | float | mm | `1.6` | slot width (>= 1.5 for reliable bridging / cleaning) |
| `pitch` | float | mm | `4.0` | centre-to-centre across slots (web = pitch - slot_w, keep >= 1.2) |
| `slot_l` | float | None | mm | `auto` | slot length; default fills area_l |
| `rows` | int | count | `1` | number of slot columns along the length; default 1 |
| `rotation` | float | deg | `0.0` | rotate the whole pattern in-plane |

```python
side = side - extrude(Plane.XZ * vent_slots(40, 16), amount=wall, both=True)
```

## models

| project | components used | golden |
|---|---|---|
| `esp32_devkit_case` | `fasteners.clearance_hole`, `fasteners.heat_set_boss`, `primitives.box_lid`, `primitives.cable_grommet`, `primitives.pcb_slot_cradle`, `primitives.rounded_box`, `primitives.vent_slots` | yes |
| `l_bracket_40` | `primitives.l_bracket` | yes |
